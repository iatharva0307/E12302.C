#include <stdio.h>
#include <conio.h>

void main()
{
    int a, b, sum;

    clrscr();

    printf("Enter first number: ");
    scanf("%d", &a);

    printf("Enter second number: ");
    scanf("%d", &b);

    sum = a + b;

    printf("Sum = %d", sum);

    getch();
}
